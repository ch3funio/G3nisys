# automation:
# # - alias: au_my_scene
  # # trigger:
  # # - platform: state
    # # entity_id: input_select.my_scene
  # # action:
  # # - condition: state
    # # entity_id: group.gr_who
    # # state: home
  # # - service: scene.turn_on
    # # data_template:
      # # entity_id: "{{ 'scene.sc_{}'.format(states.input_select.my_scene.state) }}"
# - alias: au_automatyczna_pora_dnia
  # mode: restart
  # trigger:
  # - platform: state
    # entity_id: sensor.pora_dnia
  # - platform: state
    # entity_id: input_boolean.tryb_spania
    # to: "off"
  # - platform: homeassistant
    # event: start
  # action:
  # - condition: or
    # conditions:
    # - condition: template
      # value_template: "{{ is_state('input_boolean.tryb_spania', 'off') }}"
    # - condition: template
      # value_template: "{{ is_state('input_boolean.tryb_spania', 'on') and is_state('sensor.pora_dnia', 'dawn') }}"
  # - service: input_select.select_option
    # data_template:
      # entity_id: input_select.my_scene
      # option: "{{ states('sensor.pora_dnia') }}"
  # - service: input_boolean.turn_off
    # entity_id: input_boolean.tryb_spania